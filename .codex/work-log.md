# 作業記録

## 2026-09-25

- 何をしたか：`criteria.xlsx` と `icl.xlsx` の構造を確認し、両ファイルを追跡対象から外す `.gitignore`、README、データセット設計案を作成した。
- なぜそうしたか：WCAGとJISの版、名称の変遷、実務上の確認条件を混同せず、後から複数のツールで再利用できる参照基盤にするため。
- 未完了：データ本体の生成、各訳語の出典確認、JISとICLの公開範囲の確認。
- 次にやるとよいこと：出典台帳と項目ID一覧を作り、`criteria.xlsx` の117行を照合する。

## 2026-09-25：データ本体の作成

- 何をしたか：W3CのWCAG 2.1/2.2公式JSON、WCAG 2.2固定版、WAICの日本語訳、手元の `criteria.xlsx` を照合し、`data/` に出典、項目、名称、本文、対応関係、一覧CSVを生成した。再生成・検証スクリプトを追加した。
- なぜそうしたか：規格版と名称の由来を分け、旧訳や廃止項目を失わずに参照できるようにするため。
- 未完了：WCAG 2.0原文、JIS本文と名称の正式な照合、`icl.xlsx` の実務知識、公開用の権利確認。
- 次にやるとよいこと：データ内容を人がサンプル確認し、JIS名称の出典と公開範囲を確認する。

## 2026-09-25：予定名称の表記（後に公開データから除外）

- 何をしたか：手元資料の予定名称を比較用に整形した。現在は公開用データから除外し、復元用データだけをGit管理対象外に保存している。
- なぜそうしたか：JIS X 8341-3:2016の名称列と同じ形式で比較できるようにするため。予定の名称を正式版と誤認させない。
- 未完了：改正JISの年と名称の確定、正式文書との照合。
- 次にやるとよいこと：改正JISの公表後に版情報と名称を再照合する。

## 2026-09-25：WCAG 2.2解説書の日英資料

- 何をしたか：WAIC日本語訳と、WAICが翻訳元として保管するW3C英語原文から、解説書HTMLを各109件、参照画像を各93件取り込んだ。コミットID、文書・画像の出典URLとSHA-256、項目との対応を台帳化し、再取り込み・検証スクリプトを追加した。
- なぜそうしたか：翻訳の照合で原文の版を固定し、画像を含む説明の根拠を再現できるようにするため。
- 未完了：画像ごとの権利表示と公開範囲の確認、現在公開中のW3C解説書との差分確認、閲覧用画面。
- 次にやるとよいこと：公開前に画像の例外的な権利表示を点検し、必要なら公開対象から外す。日英比較用の安全な閲覧画面を検討する。

## 2026-09-25：画像実データの除外

- 何をしたか：解説書が参照する画像の実ファイルを公開データから外し、`assets.json` に出典URI、相対パス、ハッシュ、参照元を残した。再取り込み処理が画像をコピーしないよう変更した。
- なぜそうしたか：画像ごとの転載条件を確認するまで、実データの再配布を避けるため。
- 未完了：画像ごとの利用条件確認と、画像が必要な場合の閲覧用画面。
- 次にやるとよいこと：URIを用いた出典へのリンクを閲覧用画面に設ける。

## 2026-09-25：未公開の予定名称を公開データから除外

- 何をしたか：予定名称の元データと生成した表示名を `names.json` と `catalog.csv` から除外した。復元用の221レコードをGit管理対象外の `.codex-private/planned-jis-names.json` に保存し、生成・検証処理も公開用の列を出力しないよう変更した。
- なぜそうしたか：未公開の可能性がある名称を公開用データセットに含めないため。
- 未完了：除外後の公開用データと解説書スナップショットは未コミット。
- 次にやるとよいこと：公開範囲を確認してから、除外後のデータをコミットする。

## 2026-09-25：Git履歴から予定名称を除去

- 何をしたか：元履歴のbundleを `.codex-private/history-before-redaction.bundle` に保存し、`data/names.json` と `data/catalog.csv` を過去コミットから除いた履歴へ `main` を切り替えた。旧reflogと到達不能オブジェクトをGit内から掃除した。
- なぜそうしたか：未公開の予定名称がGitHub公開時に過去コミットから見えないようにするため。
- 未完了：現在の作業ツリーにある除外後のJSON・CSVは未コミット。復元用bundleには旧履歴が含まれるため、引き続きGit管理対象外で保持する。
- 次にやるとよいこと：現在の公開用データを確認してコミットする。旧bundleを共有しない。

## 2026-09-25：再生成環境の整備

- 何をしたか：公開済みの名称列だけを `inputs/criteria-public.json` に固定し、私有のExcelなしで規格データを再生成できるようにした。解説書の日英リポジトリを固定コミットで取得する処理と、取得・再生成・バイト単位の差分検証を行う `tools/maintain.py` を追加した。
- なぜそうしたか：新しい作業環境でも同じ版のデータを再現し、出典や入力の変化を意図せず取り込まないため。
- 未完了：JIS名称の正式な出典照合、公開可否の確認。版更新時にはコミットIDとハッシュの見直しが必要。
- 次にやるとよいこと：`maintain.py check` を定期的に実行し、公開前に名称とライセンスを照合する。

## 2026-09-25：達成基準の参照ツール

- 何をしたか：番号を指定してWCAG各版とJISの名称・状態、収録済み本文、版間の対応、日英解説書のURLを表示する `tools/show_criterion.py` を追加した。端末表示とJSON出力に対応し、`maintain.py check` に動作確認を組み込んだ。
- なぜそうしたか：複数のJSON・CSVを手作業で行き来せず、出典と版の違いを確認できるようにするため。
- 未完了：JIS名称の正式な照合、公開可否の確認。端末表示の本文はプレーンテキスト化しておりHTML構造は表示しない。
- 次にやるとよいこと：日常の参照で使い、名称や対応関係の不備を見つけたら元データと出典を修正する。

## 2026-09-25：公開用の権利表示

- 何をしたか：独自コード等のMITライセンスを `LICENSE` に追加し、第三者由来のW3C・WAIC資料とJIS名称の出典・利用条件を `THIRD_PARTY_NOTICES.md` に整理した。READMEに適用範囲を案内し、日本語本文断片の再利用時の表示例を追加した。
- なぜそうしたか：GitHub公開時にMITが第三者の規格本文や訳文にも適用されるという誤解を避け、利用者が出典と表示条件を確認できるようにするため。
- 未完了：JIS名称と手元資料の出典・転載範囲の確認。W3C・WAIC資料の公開前の権利表示の最終確認。
- 次にやるとよいこと：公開前に上記の出典を照合し、ライセンスファイルを含む差分をレビューしてコミットする。

## 2026-09-25：JIS達成基準名の権利確認状況

- 何をしたか：JISCのFAQが許諾不要と明記する規格番号・規格名称と、データセット内の達成基準ごとの名称を区別し、後者の公開データとしての利用可否と権利者への確認が未完了であることをREADMEと権利表示に記録した。
- なぜそうしたか：JISの著作権者を原案作成団体と決めつけず、確認状況を公開前に正確に示すため。
- 未完了：`criteria.xlsx` のJIS列の由来と達成基準名の扱いについての確認。
- 次にやるとよいこと：必要に応じてJISCのFAQが案内する窓口に対象範囲と公開用途を示して相談する。

## 2026-09-25：Excel原本の撤去とWAIC版の明確化

- 何をしたか：Git管理対象外だった `criteria.xlsx` と `icl.xlsx` を作業ディレクトリから削除し、READMEの「手元の入力資料」節を削除した。Excelからの再抽出オプションと `openpyxl` 依存も削除した。WAIC日本語解説書の固定コミットが `wcag22-202609` タグと一致することを確認し、READMEに記録した。
- なぜそうしたか：再生成は `inputs/criteria-public.json` だけで可能であり、原本をこのリポジトリに置く必要がないため。データセットのドラフト状態をWAICの版と混同しないようにするため。
- 未完了：JIS達成基準名の公開条件など、このデータセット自体の公開前確認。
- 次にやるとよいこと：公開前に権利表示を見直し、差分をレビューしてコミットする。

## 2026-09-25：維持手順とAI活用ガイドの分離

- 何をしたか：READMEの再取得・検証手順を `docs/maintenance.md` に移し、READMEにはAI利用の入口を設けた。`docs/ai-usage.md` に番号での参照、RAGの検索単位とメタデータ、業務での使い分け、回答時の出典と版の扱い、Codexなどへの指示例を記した。
- なぜそうしたか：READMEから利用方法を把握しやすくし、維持担当者向けの手順とAI利用者向けの案内をそれぞれ参照できるようにするため。
- 未完了：RAGの索引生成やMCPは未実装。JIS達成基準名の公開条件は確認中。
- 次にやるとよいこと：実際のAI利用で不足するメタデータを確認し、必要になった時点で索引生成を追加する。

## 2026-09-25：公開と作業の区切り

- 何をしたか：ライセンス、権利表示、AI活用ガイド、メンテナンスガイドを含む変更を `af03e03` にコミットし、公開リポジトリ `https://github.com/jidaikobo-shibata/wcag-jis-data` の `main` にpushした。GitHubの日本語descriptionとTopicsを提案したが、設定は変更していない。
- なぜそうしたか：WCAG・JISの参照データセットを共有し、出典と利用条件、再生成手順、AIでの利用方法を確認できるようにするため。
- 未完了：JIS X 8341-3:2016の達成基準名について、公開データとしての利用可否と権利者への確認。RAGの索引生成とMCPは未実装。GitHubのdescriptionとTopicsの設定状況はこちらでは未確認。
- 次にやるとよいこと：実際の参照業務でデータとツールを使い、不足や誤りを記録する。必要に応じてJISCのFAQが案内する窓口へ達成基準名の扱いを照会する。
